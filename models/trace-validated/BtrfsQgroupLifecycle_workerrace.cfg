\* Reachability witness: is the rescan WORKER a second victim of
\* CVE-2025-39759 (free loop vs the worker's qgroup_rescan_leaf tree walk),
\* distinct from the rescan ioctl's own zero_tracking?
\*   FixFreeHoldsQgroupLock = FALSE => "Invariant NoWorkerRaceWithFree is
\*     violated": YES, reachable pre-fix (see the trail).
\* Flip the constant to TRUE and re-run to confirm the fix protects the
\* worker too (no error). See NoWorkerRaceWithFree in the .tla.
SPECIFICATION Spec
CONSTANT UserTasks = {"u1", "u2"}
CONSTANT FixFreeHoldsQgroupLock = FALSE
INVARIANT TypeOK
INVARIANT NoWorkerRaceWithFree
